// Generous whitespace scale. The light-mode Robinhood pattern relies on
// space rather than borders/cards to separate content.
export const spacing = {
  xs: 4,
  sm: 8,
  md: 12,
  lg: 16,
  xl: 24,
  xxl: 32,
  xxxl: 48,
};

// Larger pill radii suit the light-mode aesthetic — buttons, chips, period
// selectors all use `pill`. Cards still use `lg` (~20pt) for a softer edge.
export const radius = {
  sm: 10,
  md: 14,
  lg: 20,
  xl: 28,
  pill: 999,
};
